# Dependencies
web/node_modules/

# Local Claude Code settings (personal permissions, not project config)
.claude/settings.local.json

# Logs
*.log
/var/log/

# Runtime state (lives on the server, not in the repo)
/data/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Secrets — never commit these
.env
config
/etc/devbox/
SPEC.md
